# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GuessMe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GuessMe
  pod 'Alamofire', '~> 4.8.0'
  pod 'Kingfisher', '~> 5.1'

  target 'GuessMeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GuessMeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
